@*@model VehicleWashingCentersViewModel
@{
    ViewData["Title"] = "Select washing center";
    var items = (List<SelectListItem>)ViewBag.WashingCenterItems;
}

<div class="row">
    <div class="col-4">
        <form id="washingCenters" method="post">
            <input type="hidden" name="VehicleId" value="@Model.VehicleId" />
            <input type="hidden" name="RegistrationNumber" value="@Model.RegistrationNumber" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-floating">
                <h3 class="text-primary">Vehicle: @Html.DisplayFor(m => m.RegistrationNumber)</h3>
            </div>
            <div class="form-floating">
                <div class="form-group">
                    <label><b>Select washing center:</b></label>
                    <select asp-for="WashingCenterItems" asp-items="@items" class="form-control">
                    </select>
                </div>
            </div>
            <div class="row">&nbsp;</div>
            <div class="row">
                <div class="col-4">
                    <button id="login-submit" type="submit" class="btn btn-primary btn-block">Submit</button>
                </div>
                <!-- /.col -->
            </div>
        </form>
    </div>
</div>

*@